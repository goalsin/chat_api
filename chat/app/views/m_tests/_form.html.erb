<%= form_for(@m_test) do |f| %>
  <% if @m_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@m_test.errors.count, "error") %> prohibited this m_test from being saved:</h2>

      <ul>
      <% @m_test.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :desc %><br>
    <%= f.text_area :desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
